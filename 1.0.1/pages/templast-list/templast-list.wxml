<template name="templast-list">
    <view class="common-cont-list" data-numid="{{news_id}}" catchtap="ydAll">
      <view class="common-item">
        <view class="item-img"><image src="{{thumb}}"></image></view>
        <view class="item-text">
          <text>{{title}}</text>
          <text class="yuedu">阅读全部</text>
        </view>
      </view>
    </view>
</template>
